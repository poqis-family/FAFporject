using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestStartSave : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        SaveLoad.Save();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
